[build]
  command = "npm run build"
  publish = "build/client"

[context.production.environment]
  NODE_VERSION = "18"

[context.deploy-preview]
  command = "npm run build"
  publish = "build/client"
